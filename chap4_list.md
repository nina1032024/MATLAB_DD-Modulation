# OTFS 第 4.1–4.4 節 MATLAB 模擬清單

> 目標：不做 BER，先做「**理論式 vs. 模擬值**」的逐層驗證。
> 每一項都要有：(a) 理論方程式、(b) 程式實作、(c) 通過準則（數值誤差門檻）。

---

## A. 系統方程式與變數維度表

### A.1 索引與轉換慣例

| 符號 | 定義 | 維度 |
|---|---|---|
| `M` | 每個 block 的樣本數 / 延遲(delay) 格點數 | scalar |
| `N` | block 數 / 都卜勒(Doppler) 格點數 | scalar |
| `m` | 延遲索引 | `0…M-1` |
| `n` | 都卜勒索引 | `0…N-1` |
| `l` | 時頻域頻率索引 (4.2 式) | `0…M-1` |
| `k` | 時頻域時間索引 | `0…N-1` |
| `q` | 時域樣本索引，`q = m + nM` | `0…NM-1` |
| `F_M` | 么正 DFT 矩陣，`F_M[a,b] = (1/√M)e^{-j2πab/M}` | `M×M` |
| `F_N^†` | 么正 IDFT 矩陣 | `N×N` |
| `P` | row-column interleaver（置換矩陣），`PᵀP = I_NM` | `NM×NM` |
| `z` | `z = e^{j2π/(NM)}` | scalar |

### A.2 發射端（4.3.1 / 4.3.2）

| 方程式 | 說明 | 維度 |
|---|---|---|
| `X[m,n] ∈ 𝔸` | DD 域資訊符號矩陣 | `M×N` |
| `X_tf = F_M · X · F_N^†` (4.16) | ISFFT | `M×N` |
| `X̃ = F_M^† · X_tf = X · F_N^†` (4.17) | **IDZT**（delay-time 域） | `M×N` |
| `s = vec(G_tx · X̃)` (4.18) | Heisenberg + 向量化 | `NM×1` |
| `s[m+nM] = (1/√N)Σ_p X[m,p]e^{j2πpn/N}` (4.22) | 元素式 IDZT | — |
| `x = vec(Xᵀ)`，`x[n+mN] = X[m,n]` (4.31) | DD 域向量（沿 Doppler 軸切成 M 塊，每塊 N 個） | `NM×1` |
| `s = P·(G_tx ⊗ F_N^†)·x` (4.32) | 向量形式的調變 | — |
| `G_tx = G_rx = I_M` | 矩形脈波（本章假設） | `M×M` |

**注意兩種向量化方向不同**：`s` 是 `X̃` 的 column-wise vec（延遲 index 跑得快，`q=m+nM`）；`x` 是 `Xᵀ` 的 column-wise vec（都卜勒 index 跑得快，`index = n+mN`）。`P` 就是用來接這兩者。

### A.3 通道（4.1.3）

```
r[q] = Σ_{l∈ℒ} g^s[l,q] · s[q-l] + w[q],   q = 0…NM-1      (4.36)
```

整數 tap 時：

```
g^s[l,q] = Σ_{i=1}^{P} g_i · z^{κ_i (q-l)} · δ[l - l_i],    z = e^{j2π/(NM)}   (4.8)
```

分數 tap 時要加上 `sinc(l-ℓ)` 內插 (4.6)。

| 符號 | 說明 | 維度 |
|---|---|---|
| `g_i` | 第 i 路徑複數增益 | scalar |
| `l_i = τ_i·M·Δf` | 正規化延遲 tap | scalar |
| `k_i = ν_i·N·T` | 正規化都卜勒 tap | scalar |
| `l_max` | 最大延遲 tap，需 `l_max < M` | scalar |
| `G` | 時域通道矩陣，`G[q,q-l] = g^s[l,q]` (4.38) | `NM×NM` |
| `w` | AWGN，變異數 `σ_w²` | `NM×1` |

### A.4 接收端（4.3.3 / 4.3.4 / 4.4）

| 域 | 訊號關係 | 通道矩陣 | 式號 |
|---|---|---|---|
| 時域 | `r = G·s + w` | `G` (`NM×NM`) | (4.37) |
| 時頻域 | `y̌ = Ȟ·x̌ + w̌`，`x̌=(I_N⊗F_M)s` | `Ȟ=(I_N⊗F_M)·G·(I_N⊗F_M^†)` | (4.40)–(4.43) |
| 延遲-時間域 | `ỹ = H̃·x̃ + w̃`，`x̃ = Pᵀ·s` | `H̃ = Pᵀ·G·P` | (4.52)–(4.56) |
| 延遲-都卜勒域 | `y = H·x + z`，`A=(I_M⊗F_N)Pᵀ` | `H = A·G·A^† = (I_M⊗F_N)·H̃·(I_M⊗F_N^†)` | (4.58)–(4.61) |

子塊關係：`K̃_{m,l} ∈ C^{N×N}`（`H̃` 的 `(m,[m-l]_M)` 子塊，對角矩陣）、`K_{m,l} = F_N·K̃_{m,l}·F_N^†`（circulant）。

---

## B. 模擬參數清單
### B.1 系統參數

| 參數 | 建議值（驗證用） | 建議值（效能用） | 備註 |
|---|---|---|---|
| `M`（延遲格點） | 8 | 32 或 64 | 驗證階段用小值才看得到矩陣結構 |
| `N`（都卜勒格點） | 6 | 16 或 32 | 書上 Fig.4.6/4.7 就是 M=8, N=6 |
| `Δf` | 15 kHz | 15 kHz | 子載波間隔 |
| `T = 1/Δf` | 66.67 μs | — | 每個 block 時間長度 |
| `B = M·Δf` | — | — | 頻寬 |
| `T_s = 1/B = T/M` | — | — | 取樣間隔 |
| `T_f = NMT_s = NT` | — | 須 ≤ 10–20 ms | 通道在 frame 內視為固定 |
| `f_c` | 4 GHz | 4 GHz | 載波頻率（算 Doppler 用） |
| 調變 | QPSK (`Q=4`) | QPSK / 16QAM | `E_s = 1` 正規化 |
| `SNR` | — | 0:2:20 dB | 定義為 `E_s/N_0`，須明確寫清楚 |
| 蒙地卡羅次數 | 1（驗證只需 1 次） | ≥ 1e4 frames | |

### B.2 通道參數

| 參數 | 建議值 | 備註 |
|---|---|---|
| 路徑數 `P` | 3（驗證）/ 4–6（效能） | |
| 延遲 tap `l_i` | `[0, 1, 2]` | on-grid 先用整數 |
| 都卜勒 tap `k_i` | `[0, 1, -2]` 或 `[0,2,-1]` | on-grid 整數；負值要 `mod N` |
| 增益 `g_i` | 等功率 `1/√P`，或指數功率延遲剖面 | `Σ|g_i|²=1` |
| `l_max` | `max(l_i)`，需 `< M` | |
| `k_max` | `max|k_i|`，需 `< N/2` | |
| 最大車速 | 500 km/h → `ν_max = v f_c/c` | 檢查 `ν_max < Δf/2` |
| off-grid 設定 | `l_i, k_i` 取非整數（如 `1.4`, `2.5`） | 第二階段才做 |

### B.3 程式設定

- FFT 正規化：**全部用么正 (unitary) 版本**，`F = dftmtx(K)/sqrt(K)`。這是最常出錯的地方。
- 隨機種子：`rng(0)` 固定，方便重現。
- 數值誤差門檻：`relerr = norm(a-b)/norm(b) < 1e-10`。

### B.4 MATLAB 對應寫法（避免正規化寫錯）

```matlab
F_N = dftmtx(N)/sqrt(N);      F_M = dftmtx(M)/sqrt(M);
% X̃ = X * F_N'   等價於   sqrt(N)*ifft(X, [], 2)
% Y  = Ỹ * F_N   等價於   (1/sqrt(N))*fft(Y_tilde, [], 2)
% X_tf = F_M*X*F_N'  等價於  sqrt(N/M)*fft(ifft(X,[],2),[],1)
```

---

## C. 需自行推導並放進報告的中間過程（對應老師第 3 點）

依序推導，每一步都要標明矩陣維度：

1. **ISFFT ↔ 矩陣式**：由 (4.2) 的雙重求和推到 `X_tf = F_M X F_N^†`。
2. **IDZT**：由 `X̃ = F_M^† X_tf` 推到 `X̃ = X F_N^†`，並推出元素式 (4.22)。
3. **向量化與置換矩陣 `P`**：推導 `s = P(I_M ⊗ F_N^†)x`，並說明 `P` 的建構 (4.33)(4.34) 與 `PᵀP = I`。
4. **時域通道矩陣 `G`**：由連續 (4.4) → 取樣 (4.5) → 矩陣 (4.38)，說明 `l_max+1` 條次對角線的來源。
5. **時頻域**：左乘 `(I_N⊗F_M)` 推出 `Ȟ = (I_N⊗F_M)G(I_N⊗F_M^†)`，並推導 block-wise (4.46) 與 element-wise (4.48)（分離出 ICI / ISI 項）。
6. **延遲-時間域**：左乘 `Pᵀ` 推出 `H̃ = PᵀGP`，並**證明子塊 `K̃_{m,l}` 是對角矩陣**（這一步是 OTFS 低複雜度偵測的關鍵）。
7. **延遲-都卜勒域**：推出 `H = (I_M⊗F_N)H̃(I_M⊗F_N^†)`、`K_{m,l} = F_N K̃_{m,l} F_N^†`，並**證明 `K_{m,l}` 為 circulant**（用「對角矩陣經 DFT 相似變換 → circulant」的性質）。
8. **理想脈波 vs. 矩形脈波**：推導 (4.13)(4.15) 的二維循環摺積，再推導矩形脈波下多出的相位項 `z^{κ_i(m-l_i)}`，說明兩者差異來源（雙正交性喪失）。
9. **分數都卜勒洩漏**：由 (4.72) → (4.78)，推出週期 sinc 函數 `𝒟_N(x) = sin(πx)/sin(πx/N)·e^{jπx(N-1)/N}` (4.79)。
10. **雜訊統計不變性**：說明 `w, w̌, w̃, z` 因么正變換而具相同統計特性。

---

## D. 模擬項目清單（核心）

> 格式：**[目的] / [理論值] / [模擬做法] / [通過準則]**

### 階段一：無通道的自我一致性檢查（先確定 modulator/demodulator 沒寫錯）

**Sim-1　IDZT/DZT 完美重建**
- 理論：無通道無雜訊時 `Y = X`。
- 做法：`X → s → r=s → Y`，計算 `norm(Y-X,'fro')`。
- 準則：`< 1e-12`。

**Sim-2　兩條調變路徑等價**
- 理論：`ISFFT + Heisenberg` 路徑（Fig. 4.1）≡ `IDZT` 路徑（Fig. 4.3）。
- 做法：
  - 路徑A：`X_tf = F_M*X*F_N'; X̃ = F_M'*X_tf; s_A = X̃(:);`
  - 路徑B：`X̃ = X*F_N'; s_B = X̃(:);`
  - 路徑C：元素式迴圈實作 (4.22)。
- 準則：`s_A == s_B == s_C`（`<1e-12`）。接收端同理驗證 (4.27)(4.30)。

**Sim-3　置換矩陣 `P` 正確性**
- 理論：`PᵀP = I_NM`；`x̃ = Pᵀs`；`s = P(I_M⊗F_N^†)x`。
- 做法：依 (4.33)(4.34) 建 `P`，並用「寫入 `N×M` 矩陣後 row-wise 讀出」的直覺做法建第二版，兩版比對。
- 準則：兩版完全相同；且 `norm(P'*P - eye(N*M)) < 1e-12`。
- 附圖：畫 `spy(P)`，並重現 Fig. 4.10 的 interleaving 示意（M=3, N=4）。

**Sim-4　Parseval / 功率檢查**
- 理論：么正變換保能量，`||x||² = ||s||² = ||x̌||² = ||x̃||²`。
- 準則：相對誤差 `< 1e-12`。

---

### 階段二：on-grid 通道，四個 domain 的 I/O 關係驗證（老師第 4、5 點的主體）

**Sim-5　時域通道兩種實作一致（4.4.1）**
- 理論：`r = G·s`（矩陣）≡ `r[q] = Σ_l g^s[l,q]s[q-l]`（直接摺積，`q-l<0` 補零）。
- 做法：
  1. 由 `(g_i, l_i, k_i)` 依 (4.8) 產生 `g^s[l,q]`（`(l_max+1) × NM`）。
  2. 依 (4.38) 建 `G`（RZP：下三角；RCP：加上 `mod NM` 的右上角）。
  3. 寫一個 for-loop 的時變摺積函式。
- 準則：`norm(G*s - r_conv)/norm(r_conv) < 1e-12`。
- 附圖：`imagesc(abs(G))`，重現 Fig. 4.8 的三條次對角線結構。

**Sim-6　Block-wise 時域關係（4.39）**
- 理論：`r_0 = G_{0,0}s_0`；`r_n = G_{n,0}s_n + G_{n,1}s_{n-1}`。
- 做法：把 `G` 切成 `M×M` 子塊，驗證只有對角塊與第一條次對角塊非零（因 `l_max < M`）。
- 準則：其餘子塊 Frobenius norm `< 1e-12`；block-wise 結果 == 整體 `G*s`。

**Sim-7　時頻域 I/O 關係（4.4.2）**
- 理論：`y̌ = Ȟ x̌`，`Ȟ = (I_N⊗F_M)G(I_N⊗F_M^†)`，`Ȟ_{n,n'} = F_M G_{n,n'} F_M^†`。
- 做法：直接由 `r` 做 `x̌=(I_N⊗F_M)s`、`y̌=(I_N⊗F_M)r`，與 `Ȟ x̌` 比對。
- 準則：`< 1e-10`。
- 附圖：`imagesc(abs(Ȟ))` 重現 Fig. 4.9。
- **重點觀察**：計算 `Ȟ_{n,0}` 的**非對角能量佔比**（ICI）與 `Ȟ_{n,1}` 的總能量（ISI）。
  - 令 Doppler 全為 0（靜態通道）→ 驗證 `Ȟ_{n,0}` 變成對角（若用 CP）→ 印證 (4.49) 與「`G_eq` 需為 circulant」的結論。
  - 有 Doppler → ICI 出現。畫 ICI/ISI 能量對 `k_max` 的曲線。

**Sim-8　延遲-時間域 I/O 關係（4.4.3）**
- 理論：`ỹ = H̃ x̃`，`H̃ = PᵀGP`；子塊 `K̃_{m,l}` 為**對角矩陣**，對角元素為 `ν̃_{m,l}[n] = g^s[l, m+nM]`。
- 做法：
  1. `x̃ = Pᵀs; ỹ = Pᵀr;` 驗證 `ỹ == H̃x̃`。
  2. 抽出每個 `K̃_{m,l}`，檢查 off-diagonal 是否為 0。
  3. 與 (4.73) 的解析式比對 `ν̃_{m,l}`。
- 準則：`< 1e-10`；off-diagonal `< 1e-12`。
- 附圖：`imagesc(abs(H̃))` 重現 Fig. 4.11 / 4.14（注意 RZP 的右上角塊）。

**Sim-9　延遲-都卜勒域 I/O 關係（4.4.4）**
- 理論：`y = Hx`，`H = (I_M⊗F_N)H̃(I_M⊗F_N^†)`；`K_{m,l} = F_N K̃_{m,l} F_N^†` 為 **circulant**；`ν_{m,l}[k] = (1/N)Σ_n ν̃_{m,l}[n]e^{-j2πkn/N}` (4.77)。
- 做法：
  1. `y = (I_M⊗F_N)Pᵀ r`，與 `H x` 比對。
  2. 對每個 `K_{m,l}` 檢驗 circulant：`norm(K - circ(K(:,1)))`。
  3. `Y = reshape(y, N, M).'`，與 demodulator 直接算出的 `Y` 比對。
- 準則：`< 1e-10`。
- 附圖：`imagesc(abs(H))` 重現 Fig. 4.12 / 4.15。

**Sim-10　符號級 DD 域理論式驗證（最終驗收）**
- 理論（矩形脈波、整數 tap、RCP，(4.120)(4.121)）：
  ```
  Y[m,n] = Σ_i g_i · z^{k_i(m-l_i)} · X[[m-l_i]_M, [n-k_i]_N]        , m ≥ l_i
  Y[m,n] = Σ_i g_i · z^{k_i([m-l_i]_M)} · e^{-j2πn/N} · X[…]          , m <  l_i
  ```
- 做法：寫一個純 for-loop 的「理論產生器」直接由 `X` 與 `(g_i,l_i,k_i)` 算 `Y_theory`，與經過 `G` → DZT 得到的 `Y_sim` 比對。
- 準則：`norm(Y_sim - Y_theory)/norm(Y_theory) < 1e-10`。
- **這一項就是老師講的「理論值 vs. 模擬」的正式驗收。**

**Sim-11　理想脈波 vs. 矩形脈波比較（4.2 節）**
- 理論（理想脈波，(4.15)）：`Y = H_dd ⊛ X`（純二維循環摺積，**無相位項**）
  ```
  Y_ideal[m,n] = Σ_i g_i X[[m-l_i]_M, [n-k_i]_N]
  ```
- 做法：另外實作二維循環摺積版本，與 Sim-10 的矩形脈波結果相減。
- 觀察重點：
  - 差異僅來自相位 `z^{k_i(m-l_i)}` 與 `m<l_i` 的角落項。
  - 當所有 `k_i = 0` 時兩者應完全相同 → 可作為 sanity check。
  - 畫出 `|Y_rect - Y_ideal|` 的 heatmap，說明「時不變 2D 循環摺積」與「時變係數」的差別。

**Sim-12　單路徑位移測試（強烈建議先做）**
- 設 `P=1, g_1=1`，`X` 只放一個 `1`（delta 在 `(m_0,n_0)`）。
- 理論：`Y` 只在 `([m_0+l_1]_M, [n_0+k_1]_N)` 有值，大小為 `|g_1|`，相位為 `z^{k_1(m-l_1)}`。
- 這是最直觀的除錯工具：**可以立刻看出 delay/Doppler 軸有沒有搞反、正負號有沒有錯。**
- 分別測：純延遲（`k_1=0`）、純都卜勒（`l_1=0`）、兩者皆有。

**Sim-13　雜訊統計一致性**
- 理論：`w, w̌, w̃, z` 皆為 `CN(0, σ_w² I)`。
- 做法：產生 1e5 組雜訊，估計各域的變異數與相關矩陣。
- 準則：變異數相對誤差 `< 1%`；相關矩陣接近 `σ_w² I`。

---

### 階段三：off-grid（分數延遲 / 分數都卜勒）

**Sim-14　分數都卜勒的洩漏驗證**
- 設定：`l_i` 整數、`k_i` 非整數（如 `2.5`）。
- 理論 (4.80)：`ν_{m,l}[k] = (1/N)Σ_κ ν_l(κ) z^{κ(m-l)} 𝒟_N(κ-k)`，其中 `𝒟_N` 為 (4.79) 的週期 sinc。
- 做法：由 `G` 一路算出 `ν_{m,l}`，與解析式比對。
- 附圖：**重現 Fig. 4.16** — 上圖 `κ=2`（整數，單一 tap），下圖 `κ=2.5`（洩漏到所有 Doppler bin），並疊上 `𝒟_N(·)` 的虛線包絡。
- 準則：`< 1e-10`。

**Sim-15　分數延遲的洩漏驗證**
- 設定：`l_i` 非整數（如 `1.4`）。
- 理論 (4.6)：`g^s[l,q]` 含 `sinc(l-ℓ)` 內插，能量洩漏到相鄰 delay tap。
- 做法：`l` 的範圍要**擴大到 `l_max` 以外**（例如 `0…l_max+8`），否則會截斷洩漏能量而使誤差變大。
- 觀察：計算「有效路徑數 `S`」隨 fractional 程度變化，驗證 `S ≥ P`（4.6.1 節末段）。

**Sim-16　通道稀疏度 `S` 分析**
- 定義：`H` 每列的非零（或高於門檻）元素個數。
- 觀察：`S` vs. `N`（增加 `NT` 使 `S → P`）、`S` vs. 分數程度。
- 這一項可直接支撐後面第 6 章偵測複雜度的討論。

---

### 階段四（選做，銜接 4.5 與第 6 章）

**Sim-17　OTFS 變體結構比較**：RZP / RCP / CP / ZP 四種 `G` 建構方式，畫出各自的 `G, H̃, H` 結構圖（Fig. 4.8/4.17/4.18/4.19），並用 Table 4.2、4.3 驗證子塊形式。

**Sim-18　BER 初步**：on-grid + LMMSE (`x̂ = (HᴴH+σ²I)^{-1}Hᴴy`)，與 OFDM 在相同 Doppler 下比較，觀察 OTFS 的分集增益。

---

## E. 書中 Appendix C 程式碼對照（哪些能直接用、哪些要自己寫）

### E.1 對照表

| Code | 內容 | 對應章節 / 式子 | 對應 Sim | 可直接用？ |
|---|---|---|---|---|
| **1** | OTFS frame 參數（N, M, Fn, Δf, T, fc, 解析度） | 4.1、4.1.1、(4.1) | B 參數表 | ✅ 建議把 `Fn/norm(Fn)` 改寫成 `Fn/sqrt(N)`，意思一樣但清楚 |
| **2** | 產生 `X (M×N)` 與 `x = reshape(X.',NM,1)` | 4.1.2、(4.31) | Sim-1 | ✅ |
| **3** | ISFFT/IDZT 調變 + `P` 矩陣三種方法 | 4.3.1、4.3.2、(4.19)(4.20)(4.33)(4.35) | **Sim-2、Sim-3** | ✅ 三個 Method 剛好就是 Sim-2 的比對對象 ⚠️ 見 E.3 |
| **4** | 最大速度 → `ν_max`、`k_max` | 4.1.1、4.1.3 | B.2 | ✅ |
| **5** | EPA / EVA / ETU 標準通道 | 第 2 章 2.5.1 | B.2 | ✅（但見 E.3 第 5 點） |
| **6** | 由標準通道產生 `g_i, l_i, k_i` | 4.1.3、(4.7) | B.2 | ⚠️ `k_i` 預設是**分數**，屬 off-grid |
| **7** | 合成通道參數（自訂 taps, l_max, k_max） | 4.1.3 | B.2 | ⚠️ 同上，`k_i` 為分數 |
| **8** | `g^s[l,q]` (TDL) + 時域通道矩陣 `G` | 4.1.3 (4.8)、4.4.1 (4.38) | **Sim-5** | ✅ 這是 **RZP 版**（下三角），與 4.4 節假設一致 |
| **9** | `H̃ = PᵀGP`、`H = (I_M⊗F_N)H̃(I_M⊗F_N†)` | 4.4.3 (4.55)、4.4.4 (4.60) | **Sim-8、Sim-9** | ⚠️ 第一行疑似有誤，見 E.3 |
| **10** | **四種方式產生 `r`**（TDL 摺積 / `G` / `H̃` / `H`） | 4.4.1、4.4.3、4.4.4 | **Sim-5、Sim-8、Sim-9** | 🎯 **這段就是老師要的「跨 domain 一致性驗證」骨架** ⚠️ 有 off-by-one |
| **11** | AWGN 與 SNR 定義 | 4.4 (4.36) | Sim-13 | ✅ |
| **12** | 解調三種方法（Wigner / DZT / 向量式） | 4.3.3、4.3.4、(4.24)(4.27)(4.30) | **Sim-2** | ✅ |
| **13** | DD 域 LMMSE 偵測 | 第 6 章 (6.18) | Sim-18（選做） | ⚠️ 見 E.3 |
| **14** | 時域 LMMSE 偵測 | 第 6 章 (6.19) | Sim-18（選做） | ⚠️ 同上 |
| **15** | RZP-OTFS 的 `G` | 4.5.1 | 4.4 的預設情況 | ✅ 內容與 code 8 相同 |
| **16** | RCP-OTFS 的 `G` (4.83) | 4.5.2 | **Sim-10、Sim-17** | ✅ 想驗證 circulant 性質時改用這個 |
| **17** | CP-OTFS 的 `G` (4.93) | 4.5.3 | Sim-17 | ⚠️ 見 E.3 |
| **18** | ZP-OTFS 的 `G` (4.109) | 4.5.4 | Sim-17 | ✅ |

**結論：4.1–4.4 大約有 7 成可以直接沿用 code 1–12，缺的都集中在「時頻域」與「理論值產生器」。**

### E.2 Appendix C 沒有、要自己補的部分

| 缺的東西 | 對應章節 | 對應 Sim |
|---|---|---|
| ISFFT 顯式寫法 `X_tf = F_M·X·F_N†` | 4.3.1 (4.16) | Sim-2 |
| **整個時頻域**：`x̌=(I_N⊗F_M)s`、`Ȟ=(I_N⊗F_M)G(I_N⊗F_M†)` | 4.4.2 (4.40)(4.42) | **Sim-7** |
| ICI / ISI 能量分析（(4.48) 的三項拆解） | 4.4.2 | Sim-7 |
| 子塊抽取：`K̃_{m,l}`（對角性）、`K_{m,l}`（circulant 性） | 4.4.3、4.4.4 | Sim-8、Sim-9 |
| `ν̃_{m,l}[n]`、`ν_{m,l}[k]` 的解析式比對 | (4.73)(4.77) | Sim-8、Sim-14 |
| **理想脈波的 2D 循環摺積** `Y=Σ g_i X[[m-l_i]_M,[n-k_i]_N]` | 4.2.2 (4.15) | **Sim-11** |
| **符號級理論值產生器** (4.118)–(4.122) | 4.6.2 | **Sim-10** |
| 分數延遲的 `sinc(l-ℓ)` 內插（code 8 只做整數延遲） | 4.1.3 (4.6) | Sim-15 |
| 所有結構圖 `imagesc/spy` | Fig. 4.8–4.12 | G 節圖表清單 |
| 誤差總表輸出 | — | `test_all.m` |

### E.3 程式碼中要留意 / 建議修正的地方

以下幾點**不用先信我，直接跑 Sim-5、Sim-8 就會現形**，這也正好示範老師講的「模擬是用來檢查自己的理解對不對」：

1. **Code 10 Method 1 的 off-by-one**
   ```matlab
   for ell=0:(delay_spread-1)   % ← 少算最後一個 tap
   ```
   Method 2 的 `G*s.'` 是算到 `delay_spread` 的。兩者**不會相等**。應改成 `for ell=0:delay_spread`。這就是 Sim-5 要抓的東西。

2. **Code 9 第一行與 (4.55) 不一致**
   ```matlab
   H_tilda = P*G*P.'      % 書上 (4.55) 是 H̃ = Pᵀ·G·P
   ```
   下一行的 `H` 用的是 `P'*G*P`，code 10 Method 3 也是 `r = P*y_tilda`（隱含 `x̃ = Pᵀs`）。所以第一行應為 `H_tilda = P.'*G*P`。兩種都跑一次、跟 `ỹ = Pᵀr` 比對就知道哪個對。

3. **Code 3 三個 Method 的輸出形狀不一致**
   - Method 1：`s = reshape(X_tilda,1,N*M)` → **1×NM 列向量**
   - Method 2/3：→ **NM×1 行向量**

   所以 code 10、15–18 才會寫 `r = G*s.'`。建議全部統一成行向量（`s = X_tilda(:)`），並把 `G*s.'` 改成 `G*s`，否則後面很容易錯。

4. **Code 6 / 7 預設是 off-grid**
   ```matlab
   k_i = k_max*cos(2*pi*rand(1,taps));      % code 6，分數
   k_i = k_max-2*k_max*rand(1,taps);        % code 7，分數
   ```
   老師要求「先從 on-grid 開始」，所以**第一階段請加 `k_i = round(k_i);`**，做完 Sim-1~Sim-13 再拿掉去做 Sim-14。延遲 `l_i` 兩份 code 本來就是整數。

5. **Code 5 + 6 的延遲解析度不足**
   以 `M=64, Δf=15 kHz` 計算，`delay_resolution = 1/(M·Δf) ≈ 1.04 μs`。EVA 的 9 條路徑經 `round(delays/delay_resolution)` 後會塌成 `l_i = [0 0 0 0 0 1 1 2 2]`，只剩 3 個相異 delay tap。
   → 驗證階段建議直接用 **code 7 的合成通道**（可控 `l_max`, `k_max`），要用 EVA 再把 `M` 加大（如 512）或改用 ETU。

6. **Code 13 的 LMMSE 加法有 MATLAB 語意問題**
   ```matlab
   x_hat = (H'*H + sigma_w_2)^(-1) * (H'*y);
   ```
   MATLAB 中「矩陣 + 純量」是**加到每一個元素**，不是只加對角線。應為 `+ sigma_w_2*eye(N*M)`。code 14 同樣問題。（第 6 章才用到，但先記著。）

7. **Code 17 (CP-OTFS) 的 `gs` 索引值得自己驗一次**
   書上 (4.93) 是 `g^s[l, m + n(M+L_CP)]`，code 寫的是 `gs(ell+1, m+n*M+l_cp+1)`，也就是 `m + nM + L_CP`。兩者在 `n>0` 時不同。做 Sim-17 時記得比對。

8. **`Fn = dftmtx(N)/norm(Fn)`**：`norm()` 取的是譜範數，對未正規化的 DFT 矩陣恰為 `√N`，所以結果是對的么正矩陣。只是寫法不直覺，建議改 `dftmtx(N)/sqrt(N)`。

### E.4 用 Appendix C 拼出「最小可跑驗證腳本」

驗證階段建議的組裝順序（把 `N=16, M=64` 先改小成 `N=6, M=8` 比較看得到結構）：

```matlab
%% 參數
code 1                      % N, M, Fn, delta_f, T, fc, 解析度
N = 6; M = 8;               % 驗證階段用小尺寸，重算 Fn

%% 發射
code 2                      % X (M×N), x (NM×1)
code 3                      % P, s  ← 三個 Method 全跑，互相比對 (Sim-2, Sim-3)

%% 通道（on-grid）
code 7 + k_i = round(k_i);  % 合成通道，強制整數 Doppler (Sim 階段二)
code 8                      % gs, G   ← RZP 版
code 9 (修正成 P.'*G*P)      % H_tilda, H

%% 過通道
code 10 (Method 1 修 off-by-one)
  → 四種 r 互相比對          % ★ Sim-5 / Sim-8 / Sim-9 的核心

%% 接收
code 11                     % AWGN（驗證時先設 noise=0）
code 12                     % 三個 Method 全跑，互相比對 (Sim-2)

%% 自己補的部分
Sim-7  時頻域 Ȟ
Sim-10 理論值產生器 (4.118)
Sim-11 理想脈波 2D 循環摺積 (4.15)
```

跑到這裡，你手上就有 **`r` 的 4 種算法 × `Y` 的 3 種算法 + 1 個理論值**，任兩兩相減都應該 `< 1e-10`。這張誤差表放進進度報告，老師第 5 點就完整交代了。

---

## F. 建議程式檔案結構

右欄標示可從 Appendix C 哪一段改寫（🆕 = 書上沒有，要自己寫）：

```
otfs_params.m            % params struct（M,N,df,T,fc,...）        ← code 1, 4
gen_dd_symbols.m         % QPSK → X (M×N), x (NM×1)                ← code 2
gen_perm_matrix.m        % P (NM×NM)                                ← code 3
otfs_modulate.m          % X → s；method='isfft'|'idzt'|'vec'       ← code 3 (三法)
otfs_demodulate.m        % r → Y；method='dzt'|'vec1'|'vec2'        ← code 12 (三法)
gen_channel_taps.m       % (g_i,l_i,k_i)；grid='on'|'off'           ← code 5,6,7
gen_gs.m                 % g^s[l,q] ((l_max+1)×NM)，含 sinc 內插     ← code 8 + 🆕sinc
gen_G.m                  % variant='RZP'|'RCP'|'CP'|'ZP'            ← code 15,16,17,18
apply_channel_conv.m     % 直接摺積版（對照 G*s）                    ← code 10 M1 (修 off-by-one)
build_channel_matrices.m % 🆕 回傳 Ȟ, H̃, H 及子塊 K̃_{m,l}, K_{m,l}  ← code 9 (修) + 🆕Ȟ
theory_dd_relation.m     % 🆕 依 (4.118)-(4.122) 算 Y_theory
ideal_pulse_relation.m   % 🆕 依 (4.15) 算 2D 循環摺積 Y_ideal
add_awgn.m               % AWGN + SNR 定義                          ← code 11
test_all.m               % 🆕 跑 Sim-1~Sim-16，輸出相對誤差表
plot_figures.m           % 🆕 結構圖 + Fig.4.16 重現圖
```

`test_all.m` 建議輸出成一張表：

| 測項 | 相對誤差 | 通過 |
|---|---|---|
| Sim-1 完美重建 | 3.2e-16 | ✔ |
| … | … | … |

進度報告直接貼這張表，最能回應老師第 5 點。

---

## G. 報告需附的圖表清單

1. `spy(P)` 與 row-column interleaving 示意（Fig. 4.10）
2. `|G|` 結構圖（Fig. 4.8）
3. `|Ȟ|` 結構圖 + ICI/ISI 能量分析（Fig. 4.9）
4. `|H̃|` 結構圖，標出 `K̃_{m,l}` 對角性（Fig. 4.11）
5. `|H|` 結構圖，標出 `K_{m,l}` circulant 性（Fig. 4.12）
6. 單路徑 delta 測試的 `|X|` 與 `|Y|` 對照圖（Sim-12）
7. 整數 vs. 分數 Doppler 的 `ν_{m,l}[k]` 圖（重現 Fig. 4.16）
8. 理想脈波 vs. 矩形脈波的 `|Y|` 差異 heatmap
9. 誤差總表（上節）

---

## H. 最容易踩的坑

1. **FFT 正規化不一致** — 書上全是么正 DFT；MATLAB 的 `fft/ifft` 不是。所有 `√M`、`√N` 都要補齊，否則 Sim-1 就過不了。
2. **兩種 vec 方向搞混** — `s` 用 `X̃(:)`（延遲快），`x` 用 `reshape(X.',[],1)`（都卜勒快）。`P` 存在的唯一理由就是這件事；書上腳註 1 也特別提醒與文獻 [5] 的慣例不同。
3. **`z = e^{j2π/(NM)}` 不是 `e^{j2π/N}`** — 相位項的分母是 `NM`。
4. **負的 Doppler tap** — 索引要 `mod(k, N)`，`ν_{m,l}[k]` 的 `k` 必須落在 `[0,N-1]`。
5. **`m < l` 的角落塊** — RZP 與 RCP 的公式不同（(4.76) vs (4.89)），第一個 block 的行為也不同（RZP 無 IBI，RCP 有來自 CP 的干擾）。決定用哪個 variant 後要全程一致。
6. **分數延遲時 `l` 的範圍** — 必須超過 `l_max`，不然 sinc 尾巴被截斷。
7. **4.4 節本身沒有 CP/ZP per block** — 它是「單一 frame 孤立分析」，等價於 RZP-OTFS。若要得到乾淨的 circulant `K_{m,l}`（Table 4.2 中 CP 那一列），要到 4.5 的 CP-OTFS。報告中最好講清楚你模擬的是哪一種。
8. **`Δf` 與 Doppler 解析度** — 檢查 `τ_max < T` 且 `ν_max < Δf/2`；`k_max < N/2` 才不會 aliasing。
9. **不要盲抄 Appendix C** — 書上的參考碼至少有 3 處與正文式子對不起來（見 E.3）。它是「起點」不是「答案」，每一段都要用 D 節的驗證項目過一次。這件事本身也很適合寫進報告：**你抓到書上程式與 (4.55)、(4.36) 的不一致，並用數值驗證證明哪一版才對。**

---

## I. 建議執行順序

```
週次1: A + B + C（推導、參數表）→ Sim-1 ~ Sim-4
週次2: Sim-5 ~ Sim-9（四個 domain 的矩陣驗證）
週次3: Sim-10 ~ Sim-13（理論式驗收 + 理想/矩形脈波比較）
週次4: Sim-14 ~ Sim-16（off-grid）
之後:  Sim-17 ~ Sim-18（變體與 BER）
```

先把 Sim-12（單路徑 delta）做出來，之後所有除錯都會快很多。